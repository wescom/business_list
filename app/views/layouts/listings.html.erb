<!DOCTYPE html>
<html>
	<head>
		<title><%= yield :title %>Bulletin Business Listings</title>
		<%= csrf_meta_tags %>
		<%= csp_meta_tag %>

		<%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
		<%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
	</head>

	<body class="body_defaults">
		<div class="listings_content_block">
			<%= yield %>
		</div>
	</body>
</html>
